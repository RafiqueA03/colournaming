# Docker build arguments for matching host user permissions
# Set these to your user's UID and GID to avoid permission issues with mounted volumes
# Run `id -u` and `id -g` to get your values
USER_ID=1000
GROUP_ID=1000
